info:
  name: Quiz
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/quiz/{{quizId}}
  headers:
    - name: Content-Type
      value: application/json
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
